<form method="post" action="{{ path('app_oc_produit_lunettes_delete', {'id': oc_produit_lunette.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oc_produit_lunette.id) }}">
    <button class="btn">Delete</button>
</form>
