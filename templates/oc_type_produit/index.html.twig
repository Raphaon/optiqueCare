{% extends 'base.html.twig' %}

{% block title %}OcTypeProduit index{% endblock %}

{% block body %}
    <h1>OcTypeProduit index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>CodeType</th>
                <th>TypeName</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for oc_type_produit in oc_type_produits %}
            <tr>
                <td>{{ oc_type_produit.id }}</td>
                <td>{{ oc_type_produit.codeType }}</td>
                <td>{{ oc_type_produit.typeName }}</td>
                <td>
                    <a href="{{ path('app_oc_type_produit_show', {'id': oc_type_produit.id}) }}">show</a>
                    <a href="{{ path('app_oc_type_produit_edit', {'id': oc_type_produit.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_oc_type_produit_new') }}">Create new</a>
{% endblock %}
