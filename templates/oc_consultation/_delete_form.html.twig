<form method="post" action="{{ path('app_oc_consultation_delete', {'id': oc_consultation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oc_consultation.id) }}">
    <button class="btn btn-danger">
		<i class="far fa-trash-alt"></i>
		Supprimer
	</button>
</form>
