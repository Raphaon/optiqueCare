{% extends 'base.html.twig' %}

{% block title %}BulletinPrescriptions{% endblock %}

{% block body %}
    <h1>BulletinPrescriptions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bulletin_prescription.id }}</td>
            </tr>
            <tr>
                <th>DatePrescription</th>
                <td>{{ bulletin_prescription.datePrescription ? bulletin_prescription.datePrescription|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Posologie</th>
                <td>{{ bulletin_prescription.posologie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bulletin_prescriptions_index') }}">back to list</a>

    <a href="{{ path('app_bulletin_prescriptions_edit', {'id': bulletin_prescription.id}) }}">edit</a>

    {{ include('bulletin_prescriptions/_delete_form.html.twig') }}
{% endblock %}
