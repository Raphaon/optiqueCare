{% extends 'base.html.twig' %}

{% block title %}BulletinPrescriptions index{% endblock %}

{% block body %}
    <h1>BulletinPrescriptions index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DatePrescription</th>
                <th>Posologie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for bulletin_prescription in bulletin_prescriptions %}
            <tr>
                <td>{{ bulletin_prescription.id }}</td>
                <td>{{ bulletin_prescription.datePrescription ? bulletin_prescription.datePrescription|date('Y-m-d') : '' }}</td>
                <td>{{ bulletin_prescription.posologie }}</td>
                <td>
                    <a href="{{ path('app_bulletin_prescriptions_show', {'id': bulletin_prescription.id}) }}">show</a>
                    <a href="{{ path('app_bulletin_prescriptions_edit', {'id': bulletin_prescription.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_bulletin_prescriptions_new') }}">Create new</a>
{% endblock %}
