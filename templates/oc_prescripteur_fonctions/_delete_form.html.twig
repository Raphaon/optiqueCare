<form method="post" action="{{ path('app_oc_prescripteur_fonctions_delete', {'id': oc_prescripteur_fonction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oc_prescripteur_fonction.id) }}">
    <button class="btn">Delete</button>
</form>
