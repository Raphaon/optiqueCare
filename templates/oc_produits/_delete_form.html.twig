<form method="post" action="{{ path('app_oc_produits_delete', {'id': oc_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oc_produit.id) }}">
    <button class="btn">Delete</button>
</form>
