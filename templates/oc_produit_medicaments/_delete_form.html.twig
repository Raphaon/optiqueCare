<form method="post" action="{{ path('app_oc_produit_medicaments_delete', {'id': oc_produit_medicament.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oc_produit_medicament.id) }}">
    <button class="btn">Delete</button>
</form>
