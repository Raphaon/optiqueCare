{% extends 'base.html.twig' %}

{% block title %}OcProduitMedicaments index{% endblock %}

{% block body %}
    <h1>OcProduitMedicaments index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>RefMedoc</th>
                <th>DateExp</th>
                <th>NumLot</th>
                <th>DateAchat</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for oc_produit_medicament in oc_produit_medicaments %}
            <tr>
                <td>{{ oc_produit_medicament.id }}</td>
                <td>{{ oc_produit_medicament.refMedoc }}</td>
                <td>{{ oc_produit_medicament.dateExp ? oc_produit_medicament.dateExp|date('Y-m-d') : '' }}</td>
                <td>{{ oc_produit_medicament.numLot }}</td>
                <td>{{ oc_produit_medicament.dateAchat ? oc_produit_medicament.dateAchat|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_oc_produit_medicaments_show', {'id': oc_produit_medicament.id}) }}">show</a>
                    <a href="{{ path('app_oc_produit_medicaments_edit', {'id': oc_produit_medicament.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_oc_produit_medicaments_new') }}">Create new</a>
{% endblock %}
